//console.log(variable)

// ? Envía la declaracion de la variable al inicio del archivo 
var miVariable = 'variable'

// ? let se crea en la mísma linea, no envía la declaracion de la variable al inicio del archivo
let variable = 'variable creada con let'
variable = 'Otro valor de let'

const constante = 'Este valor es constante'
console.log(constante)